{{- if .Values.capabilityScan.enabled }}
apiVersion: yogo-ai.devopstoolkit.live/v1alpha1
kind: CapabilityScanConfig
metadata:
  name: default-scan
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-weight": "10"
spec:
  mcp:
    endpoint: http://yogo-ai:3456/api/v1/tools/manageOrgData
    authSecretRef:
      name: yogo-ai-secrets
      key: auth-token
  {{- with .Values.capabilityScan.debounceWindowSeconds }}
  debounceWindowSeconds: {{ . }}
  {{- end }}
{{- end }}
